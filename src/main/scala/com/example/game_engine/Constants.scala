package com.example.game_engine

import scalafx.scene.layout.{Background, BackgroundFill}
import scalafx.scene.paint.Color

object Constants {

  val GREY = new Background(Array(new BackgroundFill(Color.Gray, null, null)))
  val WHITE = new Background(Array(new BackgroundFill(Color.White, null, null)))
  val BLUE = new Background(Array(new BackgroundFill(Color.LightBlue, null, null)))

}
